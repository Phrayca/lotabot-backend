# URL de connexion PostgreSQL (Neon, Render, etc). Si absent, une base SQLite locale sera utilisée.
DATABASE_URL=postgresql://user:password@host/lotabot?sslmode=require
JWT_SECRET=change-moi-en-production
JWT_EXPIRE_MINUTES=43200
FRONTEND_ORIGIN=http://localhost:3000
